# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'SwiftRepositories (iOS)' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for SwiftRepositories (iOS)
  pod 'Alamofire', '~> 5.4'
  pod 'AlamofireImage', '~> 4.1'
  pod 'SVProgressHUD'

end

target 'SwiftRepositories (macOS)' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for SwiftRepositories (macOS)

end
